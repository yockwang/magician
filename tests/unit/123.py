import pymongo
import random

# client = pymongo.MongoClient('mongodb://localhost:27017')
# db = client.magician
# collection = db.player_status
#
# existing_player = collection.find_one({"name":"A"})
##if existing_player:
##    player_seat = collection.find
#
# turn_player = existing_player["seat"] + 1
#
##print(existing_player["seat"])
# print(turn_player)

aaa = []
a = [1, 2, 3]
b = [3, 4, 5]
aaa.append(a)
print(aaa)
aaa.append(b)
print(aaa)
